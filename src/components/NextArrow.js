import { ArrowRightCircleFill } from "react-bootstrap-icons";

function NextArrow({ onClick }) {
  return <ArrowRightCircleFill className="mt-5 arrow-next" onClick={onClick} />;
}

export default NextArrow;
